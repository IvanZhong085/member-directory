# 會員名錄網站 — 品質評分與稽核報告

> 首次基準由多代理審查產生；之後每天凌晨 03:04 由排程任務 `member-directory-nightly-qa` 自動重跑並覆蓋本檔。

## 最新一次：2026-07-06（基準）

### UI/UX 自評（8 面向，0–10）

| # | 面向 | 分數 |
|---|------|------|
| 1 | 視覺設計與美感 | 8.7 |
| 2 | 版面與資訊架構 | 8.2 |
| 3 | 導覽與流程 | 8.5 |
| 4 | 可讀性與排版 | 8.5 |
| 5 | 響應式與跨裝置 | 8.3 |
| 6 | 無障礙（WCAG AA） | 8.7 |
| 7 | 互動與回饋 | 8.5 |
| 8 | 效能與穩健性 | 8.5 |

- **平均：8.49**　**最低：8.2**
- **門檻：平均 ≥ 8.0 且最低 ≥ 7.5 → 通過 ✅**

### 本次修正（評分後套用）

1. **搜尋涵蓋服務項目／適合對象／標語**（最高影響的 findability 修正）—— 現在可用行業關鍵字（如「盆景」「罐頭塔」）找到人，不再只比對姓名/行業/編號。
2. **資料保真修正**：`陳子宸`（F 組・005）的「適合引薦對象」原本被抽取器漏掉（該頁欄位位置偏移），已依原始投影片補回「公司採購部 福利委員會／農產行老闆 常送禮客戶」。
3. **分組卡片顯示「N 位成員」**於卡面（原本只在 aria-label）。
4. **成員卡副標加上分組全名**（搜尋結果不再只顯示組別代號）。
5. **成員卡行業字級 12.5px → 14px**（達到內文可讀性標準）。
6. **加入 `:active` 按壓回饋**與 `@media (hover:none)` 觸控防殘留 hover。
7. 麵包屑首階層一致化（分組頁與成員頁皆為純文字「總覽」）。

### 先前已修（第一輪對抗式審查，22 項）

XSS 全面 `esc()` 跳脫、搜尋 debounce 殘留計時器取消、網址錯誤編碼 `URIError` 防護、對比修正（`--faint` → `#82868F`、focus ring 加深）、跳頁連結不再劫持路由、搜尋改為真實路由（Back 可用）、`aria-live` 播報搜尋筆數、SPA 導覽後焦點管理、觸控目標放大、無效 CSS 移除等。

## 防幻覺資料稽核

- **整體保真度：clean（無捏造）**
- 抽樣約 15 位成員（橫跨 12 組）逐字比對兩份原始投影片文字：姓名、編號、行業、服務、對象、標語皆能在原始檔找到，**無憑空捏造、無張冠李戴**。
- `company` 與 `business_items` 於全部 97 人皆為空字串 —— **未填入任何捏造的公司資訊**（前台顯示「待補充」屬實）。
- `dataIssue` 僅 `蕭淑芬`（F 組・000）一人為 `true`，且屬實：原始投影片該頁把她標為「梨山茶生產批發」，服務／對象／標語卻整段複製自 `鄭凱元`（115）的巧克力內容。
- 數量正確：**12 組 / 97 人**。
- 已澄清的「疑似錯誤但其實忠於原稿」：跨組重複編號（141/171/170/128）、同一人兩種姓名寫法（黃育騏/黃育麒、蔡宏毅/蔡弘毅）皆源自投影片本身，非資料漂移。
- 已修正的唯一落差：`陳子宸` 漏抽的 targets（見上，屬「漏抽」非「捏造」，已補回）。

## 已知待辦（不影響通過門檻，供夜間任務持續優化）

- 可考慮：跨組上一位/下一位、Back 還原捲動位置、A–Z 全員索引、設計 token 化與深色模式。
